import React from 'react'
import {
    PaddingContainer,
    Heading,
    FlexContainer,
    ParaText,
    IconContainer,
} from '../../styles/Global.styled';

export const Project = () => {
  return (
    <FlexContainer fullwidthchild>

    </FlexContainer>
  )
}

