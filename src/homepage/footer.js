import styled from "styled-components"

export const StyledFooter = styled.div`
    text-align: right;
    align-items: right;
    position: sticky;
    font-family: bigillabold;
    font-weight: bold;
    margin-top:1em;
`;