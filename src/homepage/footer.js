import styled from "styled-components"

export const StyledFooter = styled.div`
    text-align: right;
    align-items: right;
    position: sticky;
    font-family: bigillabold;
    margin-top: 3em;
    margin-bottom: 1em;
`;